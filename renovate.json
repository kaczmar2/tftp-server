{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "docker:pinDigests"
  ],
  "platformAutomerge": false,
  "packageRules": [
    {
      "description": "Base image digest updates are safe to merge on their own. Merging one changes the Dockerfile, which triggers the release workflow.",
      "matchManagers": ["dockerfile"],
      "matchUpdateTypes": ["digest", "pin"],
      "automerge": true
    },
    {
      "description": "Minor and patch updates to GitHub Actions are routine.",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    },
    {
      "description": "Major updates always wait for a human. Action major versions have broken this repo before.",
      "matchUpdateTypes": ["major"],
      "automerge": false
    }
  ]
}
